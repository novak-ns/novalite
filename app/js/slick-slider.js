$(document).ready(function(){
  $('.single-product-bottom-left-items').slick({
    autoplaySpeed: 2000,
    speed: 1000,
    slidesToShow: 1,
    draggable: true,
    swipeToSlide: true,
    prevArrow: '.arrow_prev',
    nextArrow: '.arrow_next',
  });
  });